# Core numerical and ML dependencies for the hallucination-detection experiments.
# Keep this file focused on direct project dependencies rather than a full
# environment freeze so the repository remains portable across machines.

numpy>=2.0,<3.0
scikit-learn>=1.5,<2.0
torch>=2.5,<3.0
transformers>=4.45,<6.0
sentencepiece>=0.2,<1.0
